ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: cluster-issuer
  hosts:
    - host: uptime-demo-1.66.co.ucll.cloud
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: uptime-demo-1-kuma-tls
      hosts:
        - uptime-demo-1.66.co.ucll.cloud
